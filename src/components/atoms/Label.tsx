export const Label = ({children}) => (
    <label
        style={{
            fontSize: '14px',
            fontWeight: '500',
            color: '#374151',
        }}
        >
        {children}
    </label>
);